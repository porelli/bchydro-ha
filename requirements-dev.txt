# Development dependencies for BC Hydro integration
#
# IMPORTANT: Requires Python 3.12 or later
# Home Assistant 2024.8+ requires Python 3.12+
#
# Quick setup:
#   python3.12 -m venv venv
#   source venv/bin/activate  # On Windows: venv\Scripts\activate
#   pip install --upgrade pip setuptools wheel
#   pip install -r requirements-dev.txt

# Type checking
mypy>=1.8.0
# types-aiohttp  # Optional - may not be available on all platforms

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-homeassistant-custom-component>=0.13.300

# Linting (optional)
# ruff>=0.1.0
# black>=23.0.0

# Home Assistant version is managed by pytest-homeassistant-custom-component
# Minimum HA 2024.8.0 required for StatisticMeanType

# Core dependencies (installed automatically with homeassistant)
aiohttp
voluptuous

# Integration dependencies
beautifulsoup4>=4.12.0
